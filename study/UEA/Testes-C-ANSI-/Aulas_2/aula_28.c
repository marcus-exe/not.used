#include <stdio.h>
#include <stdlib.h>

//---ERROS COMUNS COM PONTEIROS---
int main(int argc, char *argv[])
{     
    /*
    int var;
    int *pointer;
    int *pointer2;
    
    var = 46;
    *pointer = var; // isso está errado pois estamos inicializando o ponteiro com o valor da variável
    //o certo é fazer esse códgio sem o asterísco

    pointer = var; // o erro desse código é que estamos atribuindo uma variável ao ponteiro
    // o certo sria atribuir um caminho usando &

    printf('\n%d\n')

    */
    system("PAUSE");
    return 0;
}