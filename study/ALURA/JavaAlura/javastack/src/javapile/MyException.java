package javapile;

// those are checked
// because they are verified by the compiler

public class MyException extends Exception{
	public MyException(String mymessage) {
		super(mymessage);
	}
}
