import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navigation from "@/components/Navigation";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Marcus Sena - Software Engineer",
  description: "Software Engineering student at FIAP specializing in Mobile Development, Big Data, DevOps, and Generative AI. Explore my projects in Kotlin, Java, Python, and cloud infrastructure.",
  keywords: ["Marcus Sena", "Software Engineer", "Mobile Development", "Big Data", "DevOps", "Kotlin", "Java", "Python", "Android", "Apache Spark", "AWS", "Docker", "Kubernetes"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Navigation />
        {children}
        <Footer />
      </body>
    </html>
  );
}
